import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standalone portfolio — nothing exotic. Add image domains / bucket config
  // here when real media moves off /public.
};

export default nextConfig;
